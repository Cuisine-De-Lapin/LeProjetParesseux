package cuisine.de.lapin.simpleblockchain.repository

import cuisine.de.lapin.library.blockchain.model.BlockChainImpl
import javax.inject.Inject

class BlockRepository @Inject constructor(private val blockChain: BlockChainImpl){
    fun getBlockChain() {

    }
}